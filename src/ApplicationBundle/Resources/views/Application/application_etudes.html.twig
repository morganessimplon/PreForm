{% extends "::base.html.twig" %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/menu.css') }}" />
    <link rel="stylesheet" href="{{ asset('css/style.css') }}" />
{% endblock %}

{% block title %}{{ parent() }} - Etudes{% endblock %}

{% block menu %}{% include ':default:menu.html.twig' %}{% endblock %}

{% block body %}
<div class="col-sm-9 col-md-10 affix-content">
    <div class="container new_application">
        <div class="page-header">
            <h3><span class="glyphicon glyphicon-user"></span> Etudes</h3>
        </div>

        {{ form_start(form_application, {'attr': {'class': 'form-horizontal'}}) }}

        {{ form_errors(form_application) }}

        {# etudes form #}
        <div class="col-xs-6">
            <fieldset>
                <legend>Votre formation</legend>
                <div class="col-xs-12">
                    {{ form_label(form_application.niveauEtudes, "Niveau d'études", {'label_attr': {'class': 'col-xs-2 control-label'}}) }}
                    {{ form_errors(form_application.niveauEtudes) }}
                    <div class="col-xs-12">
                        {{ form_widget(form_application.niveauEtudes, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>


                <div class="col-xs-12">
                    <div class="col-xs-12">
                        {{ form_widget(form_application.diplomes) }}
{#                        {{ form_label(form_diplome.obtenu, "Diplôme obtnenu", {'label_attr': {'class': 'col-xs-2 control-label'}}) }}
                        {{ form_errors(form_diplome.obtenu) }}
                        <div class="col-xs-12">
                            {{ form_widget(form_diplome.obtenu, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        {{ form_label(form_diplome.file, "Diplôme", {'label_attr': {'class': 'col-xs-2 control-label'}}) }}
                        {{ form_errors(form_diplome.file) }}
                        <div class="col-xs-12">
                            {{ form_widget(form_diplome.file, {'attr': {'class': 'form-control'}}) }}
                        </div>#}
                        <a href="#" id="add_diplome" class="btn btn-default col-xs-4 col-xs-offset-2">Ajouter diplôme</a>
                    </div>
                </div>
            </fieldset>
        </div>

        <div class="col-xs-12">
            {{ form_widget(form_application) }}
            <a href="{{ path('application_situation_pro', {'id': applicant.id}) }}"><button type="button" class="btn btn-default"><i class="glyphicon glyphicon-arrow-left"> Précédent</i></button></a>
            <button type="submit" class="btn btn-default">Suivant <i class="glyphicon glyphicon-arrow-right"></i></button>
        </div>

        {{ form_rest(form_application) }}

        {{ form_end(form_application) }}

    </div>
</div>
</div>

{% endblock %}

{% block javascripts %}
    {# On charge la bibliothèque jQuery. Ici, je la prends depuis le CDN google
           mais si vous l'avez en local, changez simplement l'adresse. #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>

    {# Voici le script en question : #}
    <script type="text/javascript">
        $(document).ready(function() {
            // On récupère la balise <div> en question qui contient l'attribut « data-prototype » qui nous intéresse.
            var $container = $('div#application_etudes_diplomes');

            // On définit un compteur unique pour nommer les champs qu'on va ajouter dynamiquement
            var index = $container.find(':input').length;

            // On ajoute un nouveau champ à chaque clic sur le lien d'ajout.
            $('#add_diplome').click(function(e) {
                addDiplome($container);

                e.preventDefault(); // évite qu'un # apparaisse dans l'URL
                return false;
            });

            // On ajoute un premier champ automatiquement s'il n'en existe pas déjà un (cas d'une nouvelle annonce par exemple).
            if (index == 0) {
                addDiplome($container);
            } else {
                // S'il existe déjà des diplomes, on ajoute un lien de suppression pour chacune d'entre elles
                $container.children('div').each(function() {
                    addDeleteLink($(this));
                });
            }

            // La fonction qui ajoute un formulaire DiplomeType
            function addDiplome($container) {
                // Dans le contenu de l'attribut « data-prototype », on remplace :
                // - le texte "__name__label__" qu'il contient par le label du champ
                // - le texte "__name__" qu'il contient par le numéro du champ
                var template = $container.attr('data-prototype')
                                .replace(/__name__label__/g, 'Diplome n°' + (index+1))
                                .replace(/__name__/g,        index)
                        ;

                // On crée un objet jquery qui contient ce template
                var $prototype = $(template);

                // On ajoute au prototype un lien pour pouvoir supprimer le diplome
                addDeleteLink($prototype);

                // On ajoute le prototype modifié à la fin de la balise <div>
                $container.append($prototype);

                // Enfin, on incrémente le compteur pour que le prochain ajout se fasse avec un autre numéro
                index++;
            }

            // La fonction qui ajoute un lien de suppression d'un diplome
            function addDeleteLink($prototype) {
                // Création du lien
                var $deleteLink = $('<a href="#" id="delete_diplome" class="btn btn-danger col-xs-4 col-xs-offset-2">Supprimer</a>');

                // Ajout du lien
                $prototype.append($deleteLink);

                // Ajout du listener sur le clic du lien pour effectivement supprimer le diplome
                $deleteLink.click(function(e) {
                    $prototype.remove();

                    e.preventDefault(); // évite qu'un # apparaisse dans l'URL
                    return false;
                });
            }
        });
    </script>

{% endblock %}